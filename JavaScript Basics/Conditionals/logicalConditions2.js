if (true && false) {
    console.log('Yes!');
  } else {
    console.log('No...');
  }

  // Will output "No..." because the expression can't
  // be truthy and falsy with the AND condition.
  // So it will be falsy.