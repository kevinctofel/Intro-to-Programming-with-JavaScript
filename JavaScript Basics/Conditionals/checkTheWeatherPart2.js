const weather = 'foggy and miserable';

switch (weather) {
    case 'sunny':
        console.log(`It's a beautiful day!`);
        break;

    case 'rainy':
        console.log(`Grab your umbrealla.`);
        break;

    default:
        console.log(`Let's stay inside.`);
}

